import { GameSearchPipe } from './game-search.pipe';

describe('GameSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new GameSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
